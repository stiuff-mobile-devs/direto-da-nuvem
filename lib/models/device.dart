class Device {
  String id;
  String localization;
  String description;
  String groupId;
  DateTime createdAt;
  DateTime updatedAt;

  Device(
    this.id,
    this.localization,
    this.description,
    this.groupId,
    this.createdAt,
    this.updatedAt,
  );
}